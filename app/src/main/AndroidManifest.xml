<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="android.carpoolrider">
    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".StartFromLogIn.LogInActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".StartFromLogIn.MakeAccountActivity"/>
        <activity android:name=".StartFromLogIn.ForgotPasswordActivity"/>
        <activity android:name=".MainActivity"/>
        <activity android:name=".RequestRides.RequestNewRide.RouteActivity"/>
        <activity android:name=".Profile.ProfileActivity"/>
        <activity android:name=".RequestRides.RequestNewRide.DateActivity"/>
        <activity android:name=".RequestRides.RequestNewRide.ConfirmActivity"/>
        <activity android:name=".RequestRides.RequestNewRide.PassengerNumberActivity" />
    </application>
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
</manifest>